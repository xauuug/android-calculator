<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/color1</item>
        <item name="colorPrimaryDark">@color/color1</item>
        <item name="colorAccent">@color/color3</item>
    </style>

    <style name="Custom.Button" parent="ThemeOverlay.AppCompat">
        <item name="colorAccent">@color/color3</item>
    </style>

    <style name="Action.Button" parent="ThemeOverlay.AppCompat">
        <item name="colorAccent">@color/color4</item>
    </style>
</resources>
